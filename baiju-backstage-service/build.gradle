dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation project(":baiju-backstage-dao")
}
/*此项目为普通jar包，不为作可启动jar包*/
jar.enabled = true
bootJar.enabled = false
